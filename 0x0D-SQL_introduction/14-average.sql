-- Script: Compute Average Score

-- Description: This script computes the average score of all records in the table second_table of the hbtn_0c_0 database.

-- SQL query to compute the average score of all records
SELECT AVG(score) AS average
FROM second_table;
