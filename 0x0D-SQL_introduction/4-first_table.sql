-- Script: Create first_table

-- Description: This script creates a table called first_table in the current database of the MySQL server.

-- SQL query to create the table first_table
CREATE TABLE IF NOT EXISTS first_table (
    id INT,
    name VARCHAR(256)
);
