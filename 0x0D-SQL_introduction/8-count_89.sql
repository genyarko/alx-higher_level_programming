-- Script: Count Records with id = 89

-- Description: This script displays the number of records with id = 89 in the table first_table of the hbtn_0c_0 database.

-- SQL query to count the number of records with id = 89
SELECT COUNT(*) AS record_count
FROM first_table
WHERE id = 89;
